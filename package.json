{
  "name": "bamazon",
  "version": "1.0.0",
  "description": "A CLI-based storefront with the MySQL database backend.",
  "main": "bamazonCustomer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jacobashymo/bamazon-git.git"
  },
  "keywords": [
    "storefront",
    "CLI-storefront",
    "store"
  ],
  "dependencies": {
    "nodemon": "^1.18.4",
    "yarn": "^1.10.1"
  }
}
